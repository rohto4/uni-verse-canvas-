{
  // --------------------------------------------------------------------------
  // Gemini CLI 設定ファイル (サンプル)
  // --------------------------------------------------------------------------
  // このファイルは、プロジェクト単位でのCLIの挙動をカスタマイズするために使用します。
  //
  // 注意: このファイルをプロジェクトのルートディレクトリに `.gemini/settings.json` 
  // として配置すると、設定が自動的に読み込まれる可能性があります。
  // (ツールの仕様に依存します)
  // --------------------------------------------------------------------------

  "prompt": {
    "confirmation": {
      // ----------------------------------------------------------------------
      // コマンド実行時の承認プロンプトをスキップするコマンドのリスト。
      // 安全な読み取り専用コマンドや、頻繁に実行する定型的なコマンドを
      // 登録しておくと、開発体験が向上します。
      //
      // 警告: ファイル変更や削除を行うコマンド (rm, mv, git commitなど) の
      // 自動承認は、予期せぬ変更を引き起こすリスクがあるため慎重に追加してください。
      // ----------------------------------------------------------------------
      "allowed_commands": [
        // --------------------------------------------------------------------
        // ■ 安全な読み取り専用コマンド (高頻度で使用)
        // --------------------------------------------------------------------
        // ファイル・ディレクトリ一覧
        "ls",
        "ls -Recurse",
        "dir",
        // ファイル内容表示
        "Get-Content",
        "cat",
        // 検索
        "grep",
        "Select-String",
        // Git (状態確認)
        "git status",
        "git diff",
        "git log",

        // --------------------------------------------------------------------
        // ■ ビルド & 静的解析コマンド (高頻度で使用)
        // --------------------------------------------------------------------
        "npm run build",
        "npm run test",
        "npm run lint",
        "npx tsc",
        "npx tsc --noEmit",
        
        // --------------------------------------------------------------------
        // ■ ファイルシステム変更コマンド (注意して使用)
        // --------------------------------------------------------------------
        // 警告: 以下のコマンドはファイルシステムを変更します。
        "mkdir",
        "New-Item",
        "mv",
        "Move-Item",
        "cp",
        "Copy-Item",
        "touch",

        // --------------------------------------------------------------------
        // ■ パッケージ・リポジトリ変更コマンド (注意して使用)
        // --------------------------------------------------------------------
        // 警告: 以下のコマンドはプロジェクトの状態を変更します。
        "npm install",
        "git add"
      ]
    }
  },

  "editor": {
    // ----------------------------------------------------------------------
    // エディタやフォーマッターに関する推奨設定 (仮想)
    // ----------------------------------------------------------------------
    "default_formatter": "prettier",
    "format_on_save": true,
    "default_indent_style": "space",
    "default_indent_size": 2
  },

  "project": {
    // ----------------------------------------------------------------------
    // プロジェクト固有の情報 (仮想)
    // ----------------------------------------------------------------------
    "main_language": "TypeScript",
    "framework": "Next.js"
  }
}
