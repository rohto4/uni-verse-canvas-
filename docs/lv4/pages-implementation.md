# ページ実装状況

各ページの詳細な実装状況、使用コンポーネント、Server Actionsを記録します。

**最終更新**: 2026-02-08

---

## 📑 ページ一覧

### 公開ページ（Public）
1. [ホーム](#1-ホーム---public)
2. [読み物一覧](#2-読み物一覧---publicposts)
3. [記事詳細](#3-記事詳細---publicpostsslug)
4. [作ったもの](#4-作ったもの---publicworks)
5. [進行中のこと](#5-進行中のこと---publicprogress)
6. [自己紹介](#6-自己紹介---publicabout)
7. [関連リンク](#7-関連リンク---publiclinks)

### 管理画面（Admin）
1. [ダッシュボード](#1-ダッシュボード---adminadmindashboard)
2. [記事一覧](#2-記事一覧---adminadminposts)
3. [記事作成](#3-記事作成---adminadminpostsnew)
4. [記事編集](#4-記事編集---adminadminpostsid)
5. [記事プレビュー](#5-記事プレビュー---adminadminpostspreview)
6. [プロジェクト一覧](#6-プロジェクト一覧---adminadminprojects)
7. [進行中一覧](#7-進行中一覧---adminadminin-progress)
8. [バックアップ](#8-バックアップ---adminadminbackup)

---

## 🌐 公開ページ（Public）

### 1. ホーム - `/` (public)

**ファイル**: `src/app/(public)/page.tsx`

#### 実装状況: 🟡 部分実装（モックのみ）

#### セクション構成
```
┌─────────────────────────────────┐
│  Hero（ヒーローセクション）        │
├─────────────────────────────────┤
│  最新記事（Latest Posts）         │
├─────────────────────────────────┤
│  最新プロジェクト（Latest Works）  │
├─────────────────────────────────┤
│  進行中のこと（In Progress）       │
└─────────────────────────────────┘
```

#### 実装内容
- ✅ ヒーローセクション（静的コンテンツ）
- ✅ 最新記事セクション（モック3件）
- ✅ 最新プロジェクトセクション（モック3件）
- ✅ 進行中セクション（モック）
- ⏳ DB連携（未実装）

#### 使用コンポーネント
- `GradientAccent` - グラデーション装飾
- `AccentCard` - アクセントカード
- `Button` - ボタン

#### 必要なServer Actions（未実装）
```typescript
// 最新記事取得
const recentPosts = await getPosts({ limit: 3, sort: 'latest' })

// 最新プロジェクト取得
const recentProjects = await getProjects({ limit: 3 })

// 進行中取得
const inProgress = await getInProgressItems('in_progress')
```

---

### 2. 読み物一覧 - `/posts` (public)

**ファイル**: `src/app/(public)/posts/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────┬──────────────────────┐
│             │  PostsFilter          │
│  タグ       ├──────────────────────┤
│  フィルタ   │  PostsList            │
│             │  (記事カード × N)     │
│             ├──────────────────────┤
│             │  Pagination           │
└─────────────┴──────────────────────┘
```

#### 実装機能
- ✅ 記事一覧表示（10件/ページ）
- ✅ タグフィルタリング（AND検索）
- ✅ 検索機能（タイトル・抜粋・本文）
- ✅ ページネーション
- ✅ ソート機能（URLパラメータ）
- ✅ 結果サマリー表示
- ✅ Suspenseによる段階的レンダリング

#### 使用コンポーネント
- `PostsList` - 記事一覧表示
- `PostsFilter` - タグフィルタ・検索
- `Pagination` - ページネーション

#### 使用Server Actions
```typescript
// 記事一覧取得
const result = await getPosts({
  page: Number(searchParams.page) || 1,
  tags: searchParams.tags?.split(',') || [],
  search: searchParams.search || '',
  limit: 10,
  sort: 'latest'
})

// タグ一覧取得
const tags = await getTagsWithCount()
```

#### URLパラメータ
- `?page=1` - ページ番号
- `?tags=nextjs,react` - タグフィルタ（カンマ区切り、AND検索）
- `?search=TypeScript` - 検索キーワード
- `?sort=popular` - ソート順（latest/oldest/popular）

#### スタイル
- レスポンシブグリッド（sm: 1列、md: 2列、lg: 3列）
- スケルトンローディング
- 空の結果時のメッセージ

---

### 3. 記事詳細 - `/posts/[slug]` (public)

**ファイル**: `src/app/(public)/posts/[slug]/page.tsx`

#### 実装状況: ⏳ 未実装

#### ページ構成（予定）
```
┌────────────────────────┬──────────┐
│  タイトル・メタ情報     │          │
├────────────────────────┤  目次    │
│                        │  (TOC)   │
│  記事本文               │          │
│  (Tiptap JSON表示)      │          │
│                        │          │
├────────────────────────┴──────────┤
│  タグ・シェアボタン                 │
├────────────────────────────────────┤
│  関連記事                           │
└────────────────────────────────────┘
```

#### 実装予定機能
- ⏳ 記事本文表示（Tiptap JSONレンダリング）
- ⏳ 目次自動生成（h2/h3から）
- ⏳ 関連記事表示
- ⏳ シェアボタン（Twitter/Facebook/Link）
- ⏳ 閲覧数・読了時間表示
- ⏳ カバー画像表示
- ⏳ OGP設定

#### 必要なServer Actions
```typescript
// 記事取得（閲覧数自動インクリメント）
const post = await getPostBySlug(params.slug)

// 関連記事取得
const relatedPosts = await getRelatedPosts(post.id, 3)
```

#### 必要なコンポーネント（未作成）
- `PostContent` - 記事本文表示（Tiptap JSONレンダリング）
- `TableOfContents` - 目次
- `ShareButtons` - シェアボタン
- `RelatedPosts` - 関連記事

---

### 4. 作ったもの - `/works` (public)

**ファイル**: `src/app/(public)/works/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────┬──────────────────────┐
│             │  ProjectsFilter       │
│  タグ       ├──────────────────────┤
│  フィルタ   │  カードギャラリー      │
│             │  (プロジェクト × N)   │
└─────────────┴──────────────────────┘
```

#### 実装機能
- ✅ プロジェクト一覧表示（カードギャラリー）
- ✅ タグフィルタリング（AND検索）
- ✅ ステータスフィルタ（完了済み/アーカイブ）
- ✅ DB連携

#### 使用コンポーネント
- `ProjectsFilter` - タグフィルタ
- カードギャラリー（ページ内実装）

#### 使用Server Actions
```typescript
// プロジェクト一覧取得
const projects = await getProjects({
  status: 'completed',
  tags: searchParams.tags?.split(',') || []
})

// タグ一覧取得
const tags = await getTagsWithCount()
```

#### スタイル
- レスポンシブグリッド（sm: 1列、md: 2列、lg: 3列）
- カードホバーエフェクト
- デモURL・GitHubリンク表示

---

### 4.5 プロジェクト詳細 - `/works/[slug]` (public)

**ファイル**: `src/app/(public)/works/[slug]/page.tsx`

#### 実装状況: ✅ 実装完了（2026-02-08）

#### ページ構成
```
┌─────────────────────────────────────────────────┐
│  ヘッダー基本情報（実績の定量化）                  │
│  - タイトル・説明                                │
│  - 開発期間（start_date - end_date）              │
│  - 開発規模（steps_count）                        │
│  - 使用した生成AI（used_ai）                      │
│  - リリースページURL（demo_url）                  │
├─────────────────────────────────────────────────┤
│  ビジュアルセクション（横スクロールギャラリー）     │
│  - gallery_images を横スクロール表示              │
│  - 画像クリックで拡大表示（Lightbox）             │
├──────────────────────────────┬──────────────────┤
│  本文コンテンツ                │  技術分析         │
│  (Tiptap JSON レンダリング)     │  (円グラフ)       │
│                              │  tech_stack表示   │
│                              │                  │
├──────────────────────────────┴──────────────────┤
│  タグ・シェアボタン                                │
├─────────────────────────────────────────────────┤
│  関連記事（動的レコメンド）                        │
│  - タグ類似度 + ランダマイズで3件表示              │
└─────────────────────────────────────────────────┘
```

#### 実装機能
- ✅ プロジェクト基本情報表示
- ✅ 開発期間・規模・使用AIの表示
- ✅ 横スクロールギャラリー（Lightbox付き）
- ✅ 技術スタック円グラフ（Chart.js）
- ✅ 本文コンテンツ（Tiptap JSONレンダリング）
- ✅ 関連記事3件（タグ類似度 + ランダマイズ）
- ✅ OGP設定（タイトル・説明）
- ✅ カバー画像表示

#### 使用コンポーネント
- `ProjectGallery` - 横スクロールギャラリー + Lightbox
- `TechStackChart` - Chart.js円グラフ
- `ProjectContent` - Tiptap JSONレンダリング
- `RelatedPosts` - 関連記事表示
- `Card`, `Badge`, `Button`, `Separator` - shadcn/ui

#### 使用Server Actions
```typescript
// プロジェクト取得
const project = await getProjectBySlug(params.slug)

// 関連記事取得（内部処理）
const tagIds = project.tags.map(t => t.id)
const relatedPosts = await getRelatedPostsByTagsWithRandom(tagIds, 3)
```

#### データフロー
- `getProjectBySlug()` で全データ取得（タグ含む）
- `gallery_images` を ProjectGallery に渡す
- `tech_stack` を TechStackChart に渡す
- `content` を ProjectContent に渡す
- `tags` を RelatedPosts に渡す

#### スタイル
- レスポンシブレイアウト（グリッド: 1列 → lg: 3列）
- カード形式のセクション区切り
- グラデーション背景（bg-universe + cloud-section）

---

### 5. 進行中のこと - `/progress` (public)

**ファイル**: `src/app/(public)/progress/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────────────────────────┐
│  タブ（未着手/中断中/進行中/完了） │
├─────────────────────────────────┤
│  進行中アイテム一覧               │
│  - タイトル                      │
│  - 説明                         │
│  - プログレスバー                │
│  - ステータスバッジ               │
└─────────────────────────────────┘
```

#### 実装機能
- ✅ ステータス別タブ表示
- ✅ 進行中アイテム一覧表示
- ✅ プログレスバー表示（0-100%）
- ✅ ステータスバッジ
- ✅ 完了後のプロジェクトリンク
- ✅ DB連携

#### 使用コンポーネント
- `Tabs` - ステータス別タブ
- `Badge` - ステータスバッジ
- プログレスバー（ページ内実装）

#### 使用Server Actions
```typescript
// ステータス別取得（タブ切替時）
const items = await getInProgressItems(status)

// 全件取得（初期表示）
const all = await getInProgressItems()
```

#### スタイル
- タブ切替アニメーション
- プログレスバー（グラデーション）
- ステータス別カラー
- レスポンシブカラム（sm: 1列、lg: 2列）

---

### 6. 自己紹介 - `/about` (public)

**ファイル**: `src/app/(public)/about/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────────────────────────┐
│  プロフィール情報                 │
│  - 名前                         │
│  - 自己紹介文                    │
│  - スキル                        │
│  - 経歴                         │
└─────────────────────────────────┘
```

#### 実装機能
- ✅ プロフィール表示
- ✅ DB連携（`pages` テーブルの `metadata` から取得）
- ✅ スキルタグ表示
- ✅ 経歴タイムライン

#### 使用コンポーネント
- `Card` - セクション区切り
- `Badge` - スキルタグ

#### 使用Server Actions
```typescript
// 自己紹介ページ取得
const page = await getPage('about')
const metadata = page.metadata as {
  name: string
  title: string
  description: string
  skills: string[]
  career: Array<{ year: string; title: string; description: string }>
}
```

#### データ構造（metadata）
```json
{
  "name": "あなたの名前",
  "title": "職種・肩書き",
  "description": "自己紹介文",
  "skills": ["Next.js", "TypeScript", "React"],
  "career": [
    {
      "year": "2024",
      "title": "〇〇株式会社",
      "description": "説明文"
    }
  ]
}
```

---

### 7. 関連リンク - `/links` (public)

**ファイル**: `src/app/(public)/links/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────────────────────────┐
│  カテゴリ1（SNS）                 │
│  - リンク1                       │
│  - リンク2                       │
├─────────────────────────────────┤
│  カテゴリ2（開発）                │
│  - リンク1                       │
│  - リンク2                       │
└─────────────────────────────────┘
```

#### 実装機能
- ✅ カテゴリ別リンク一覧表示
- ✅ DB連携（`pages` テーブルの `metadata` から取得）
- ✅ アイコン表示（Lucide Icons）
- ✅ 外部リンク対応（target="_blank"）

#### 使用コンポーネント
- `Card` - カテゴリカード
- `ExternalLink` アイコン（Lucide）

#### 使用Server Actions
```typescript
// リンクページ取得
const page = await getPage('links')
const metadata = page.metadata as {
  links: Array<{
    category: string
    items: Array<{
      name: string
      url: string
      description?: string
    }>
  }>
}
```

#### データ構造（metadata）
```json
{
  "links": [
    {
      "category": "SNS",
      "items": [
        {
          "name": "GitHub",
          "url": "https://github.com/...",
          "description": "GitHubプロフィール"
        }
      ]
    }
  ]
}
```

---

## 🔧 管理画面（Admin）

### 1. ダッシュボード - `/admin/dashboard` (admin)

**ファイル**: `src/app/(admin)/admin/dashboard/page.tsx`

#### 実装状況: 🟡 部分実装（モックのみ）

#### ページ構成
```
┌────────────┬────────────┬────────────┐
│  統計1      │  統計2      │  統計3      │
│  (記事数)   │  (PV)       │  (タグ数)   │
├────────────┴────────────┴────────────┤
│  最近の記事                            │
├──────────────────────────────────────┤
│  最近のプロジェクト                     │
└──────────────────────────────────────┘
```

#### 実装内容
- ✅ 統計カード表示（モック）
- ⏳ 最近の記事一覧（モック）
- ⏳ 最近のプロジェクト一覧（モック）
- ⏳ DB連携（未実装）

#### 必要なServer Actions（未実装）
```typescript
// 統計情報取得
const stats = {
  postCount: await supabase.from('posts').select('*', { count: 'exact', head: true }),
  projectCount: await supabase.from('projects').select('*', { count: 'exact', head: true }),
  tagCount: await supabase.from('tags').select('*', { count: 'exact', head: true }),
}

// 最近の記事
const recentPosts = await getPosts({ limit: 5, sort: 'latest' })

// 最近のプロジェクト
const recentProjects = await getProjects({ limit: 5 })
```

---

### 2. 記事一覧 - `/admin/posts` (admin)

**ファイル**: `src/app/(admin)/admin/posts/page.tsx`

#### 実装状況: 🟡 部分実装（モックのみ）

#### ページ構成
```
┌─────────────────────────────────┐
│  フィルタ（ステータス・検索）      │
├─────────────────────────────────┤
│  記事一覧テーブル                 │
│  - タイトル                      │
│  - ステータス                    │
│  - 公開日                       │
│  - 編集/削除ボタン                │
└─────────────────────────────────┘
```

#### 実装予定機能
- ⏳ 記事一覧表示（テーブル形式）
- ⏳ ステータスフィルタ（全て/下書き/公開済み/予約投稿）
- ⏳ 検索機能
- ⏳ ソート機能
- ⏳ 編集・削除機能
- ⏳ ページネーション

---

### 3. 記事作成 - `/admin/posts/new` (admin)

**ファイル**: `src/app/(admin)/admin/posts/new/page.tsx`

#### 実装状況: 🟡 エディタ実装済み、保存処理未実装

#### ページ構成
```
┌─────────────────────────────────┐
│  タイトル入力                    │
├─────────────────────────────────┤
│  抜粋入力                        │
├─────────────────────────────────┤
│  Tiptapエディタ                  │
│  - ツールバー                    │
│  - 本文入力エリア                │
├─────────────────────────────────┤
│  メタデータエリア                 │
│  - カテゴリ                      │
│  - タグ                         │
│  - 公開状態                      │
├─────────────────────────────────┤
│  プレビュー | 下書き保存 | 公開   │
└─────────────────────────────────┘
```

#### 実装機能
- ✅ タイトル入力
- ✅ 抜粋入力
- ✅ Tiptapエディタ（全機能実装済み）
  - ✅ リッチテキスト編集
  - ✅ 画像挿入・リサイズ
  - ✅ 二段組レイアウト（背景色なし/水色背景）
  - ✅ テーブル挿入・削除
  - ✅ コードブロック（シンタックスハイライト）
  - ✅ YouTube埋め込み
  - ✅ リンク挿入
  - ✅ チェックリスト
- ✅ タグ入力（カンマ区切り）
- ✅ カテゴリ選択
- ✅ 公開状態選択
- ✅ プレビュー機能（別タブで開く）
- ⏳ 保存処理（未実装）

#### 使用コンポーネント
- `TiptapEditor` - メインエディタ
- `EditorToolbar` - ツールバー
- `ResizableImage` - リサイズ可能画像
- `ColumnLayout` - 二段組レイアウト
- `TableWithDelete` - 削除可能テーブル

#### 必要なServer Actions（未実装）
```typescript
// 記事作成
const post = await createPost({
  title,
  excerpt,
  content: editor.getJSON(),
  tags: tagIds,
  status: 'draft',
  // ...
})

// 画像アップロード
const imageUrl = await uploadImage(file)
```

---

### 4. 記事編集 - `/admin/posts/[id]` (admin)

**ファイル**: `src/app/(admin)/admin/posts/[id]/page.tsx`

#### 実装状況: ⏳ 未実装

#### 実装予定機能
- ⏳ 記事データ取得・表示
- ⏳ 記事作成画面と同じエディタUI
- ⏳ 更新処理
- ⏳ 削除処理

#### 必要なServer Actions（未実装）
```typescript
// 記事取得（下書きも含む）
const post = await getPostById(params.id)

// 記事更新
const updated = await updatePost(params.id, data)

// 記事削除
await deletePost(params.id)
```

---

### 5. 記事プレビュー - `/admin/posts/preview` (admin)

**ファイル**: `src/app/(admin)/admin/posts/preview/page.tsx`

#### 実装状況: ✅ 実装完了

#### ページ構成
```
┌─────────────────────────────────┐
│  コントロールバー                 │
│  - デバイス切替                  │
│  - 閉じるボタン                  │
├─────────────────────────────────┤
│  記事プレビュー                  │
│  (公開レイアウトで表示)           │
└─────────────────────────────────┘
```

#### 実装機能
- ✅ 記事プレビュー表示
- ✅ デバイス切替（Desktop/Tablet/Mobile）
- ✅ 公開レイアウトでの確認
- ✅ Header統合
- ✅ 背景デザイン統合
- ✅ 目次サイドバー表示
- ✅ localStorage経由でデータ受け渡し

#### データフロー
```typescript
// 記事作成画面 → プレビュー画面
localStorage.setItem('preview-post', JSON.stringify({
  title,
  excerpt,
  content: editor.getJSON(),
  tags,
  published_at: new Date().toISOString(),
}))

window.open('/admin/posts/preview', '_blank')
```

---

### 6. プロジェクト一覧 - `/admin/projects` (admin)

**ファイル**: `src/app/(admin)/admin/projects/page.tsx`

#### 実装状況: ✅ 実装完了（2026-02-08）

#### ページ構成
```
┌─────────────────────────────────────────────────┐
│  ヘッダー                                        │
│  - タイトル・説明                                │
│  - 新規作成ボタン                                │
├─────────────────────────────────────────────────┤
│  プロジェクト一覧（カード形式）                   │
│  - タイトル・説明                                │
│  - タグ表示                                      │
│  - 開発期間                                      │
│  - 編集・外部リンクボタン                        │
└─────────────────────────────────────────────────┘
```

#### 実装機能
- ✅ プロジェクト一覧表示（カード形式）
- ✅ 新規作成ボタン
- ✅ 編集ボタン（/admin/projects/[id]）
- ✅ 外部リンクボタン（demo_url）
- ✅ タグ表示（最大5件）
- ✅ 開発期間表示

#### 使用コンポーネント
- `Card`, `CardHeader`, `CardTitle` - shadcn/ui
- `Badge` - タグ表示
- `Button` - アクションボタン
- `Link` - ナビゲーション

#### 使用Server Actions
```typescript
const projects = await getProjects({ status: 'completed' })
```

---

### 7. プロジェクト作成 - `/admin/projects/new` (admin)

**ファイル**: `src/app/(admin)/admin/projects/new/page.tsx`

#### 実装状況: ⏳ 未実装（コンポーネントのみ準備完了）

#### 実装予定機能
- ⏳ プロジェクト作成フォーム
- ✅ TechStackInput コンポーネント（準備完了）
- ✅ ImageUploadMultiple コンポーネント（準備完了）
- ✅ Tiptapエディタ（記事作成と同じエディタを流用）
- ⏳ 保存処理（createProject() 使用）

#### 必要なコンポーネント（準備完了）
- `TechStackInput` - 技術スタック入力
- `ImageUploadMultiple` - 複数画像アップロード
- `TiptapEditor` - 本文エディタ

---

### 8. 進行中一覧 - `/admin/in-progress` (admin)

### 7. 進行中一覧 - `/admin/in-progress` (admin)

**ファイル**: `src/app/(admin)/admin/in-progress/page.tsx`

#### 実装状況: 🟡 部分実装（モックのみ）

#### 実装予定機能
- ⏳ 進行中アイテム一覧表示
- ⏳ ステータス管理
- ⏳ 編集・削除機能

---

### 8. バックアップ - `/admin/backup` (admin)

**ファイル**: `src/app/(admin)/admin/backup/page.tsx`

#### 実装状況: 🟡 部分実装（モックのみ）

#### 実装予定機能
- ⏳ エクスポート機能（JSON/Markdown）
- ⏳ インポート機能（重複検出・スキップ）
- ⏳ ZIP一括ダウンロード

---

## 🔗 関連ドキュメント

- [実装状況](./implementation-status.md) - 全体の実装状況
- [コンポーネント仕様](./component-spec.md) - UIコンポーネント
- [Server Actions仕様](../lv2/api-spec.md) - データ取得・操作
- [エディタ実装状況](./editor-implementation-status.md) - Tiptapエディタ詳細

---

**最終更新**: 2026-02-08
**次回更新予定**: 記事詳細ページ実装時
